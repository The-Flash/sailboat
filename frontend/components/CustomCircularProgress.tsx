import { CircularProgress } from "@chakra-ui/react";

export function CustomCircularProgress() {
    return (
        <CircularProgress
            size={5}
            isIndeterminate
        />
    )
}